@startuml
|a| Actor
|sys| system
|ser| server
|a|
start
:Access "Change password" screen;
repeat :Enter old password and new password;
:Click on "Change password" button;
|sys|
backward :Wrong format(message);
repeat while (check format ?) is (Format is incorrect) not (Format is correct)
:Send change password request;
|ser|
: Authenticate password;
:Save Changes;
|sys|
if (Change Status) then (Change successful)
:Change password Successful(message);
:Move user to Home screen;
else (Change fail)
:Change password Failed(message);
:Ask enter another password again;
endif

stop
@enduml