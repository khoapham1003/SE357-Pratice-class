@startuml
|a| User
|sys| system
|ser| server
|a|
start
:Enter information to sign up an account; 
:Click on "Sign up" button;
|sys|
if(Check format) then (Format is incorrect)
:Wrong format(message);
else (Format is correct)
:Send sign up request;
|ser|
:Authenticate username and password;
|sys|
if(Check user) then (Valid user)
:Reject Sign up;
:Sign up Failed(message) ;
:Ask sign up again;
else (Invalid user)
|ser|
:Save new user;
|sys|
:Accept Sign up;
:Sign up Successful(message);
:Move user to Sign in screen;
endif
endif

stop
@enduml